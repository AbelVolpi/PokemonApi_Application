<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/item_layout"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:backgroundTint="@color/light_gray"
    android:foreground="?android:attr/selectableItemBackground"
    app:cardCornerRadius="@dimen/small_card_view_corner_radius"
    app:cardElevation="@dimen/small_card_elevation">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <ImageView
            android:id="@+id/pokemon_image"
            android:layout_width="@dimen/pokemon_small_image_size"
            android:layout_height="@dimen/pokemon_small_image_size"
            android:layout_gravity="center"
            android:contentDescription="@string/empty_description"
            android:transitionName="pokemon_home_image" />

        <TextView
            android:id="@+id/pokemon_name_text_view"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:layout_marginTop="-10dp"
            android:layout_marginBottom="@dimen/margin_small"
            android:gravity="center"
            android:maxWidth="200dp"
            android:minWidth="100dp"
            android:text="@string/pokemon_name_test"
            android:textAllCaps="true" />

    </LinearLayout>

</androidx.cardview.widget.CardView>